# Chapter 1.5.1: File Copying

## Core Objectives
* Understand and use the `getchar()` and `putchar()` functions
* Redirect input stream into a file

## Technical Insights
* **Modified Run Script** To read the input from a file I rely on the shell script, thus I avoid hardcoding the input file inside of `main.c`.

## System Verification
* **Terminal Utilities** Verified that terminal utilities and input/output pipelines function properly.
